services:
  ipad-mini-ha-panel:
    build: .
    container_name: ipad-mini-ha-panel
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PANEL_PORT:-8082}:8080"
    volumes:
      - ./config:/app/config:ro
